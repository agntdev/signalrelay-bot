[
  {
    "name": "Follow shows provider not found",
    "steps": [
      {
        "send": { "callback": "follow:provider_id" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "editMessageText", "payload": { "text": "Provider not found." } }
        ]
      }
    ]
  }
]

[
  {
    "name": "Unfollow shows provider not found",
    "steps": [
      {
        "send": { "callback": "unfollow:provider_id" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "editMessageText", "payload": { "text": "Provider not found." } }
        ]
      }
    ]
  }
]

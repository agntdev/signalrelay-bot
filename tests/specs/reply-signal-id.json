[
  {
    "name": "Reply to provider shows signal not found",
    "steps": [
      {
        "send": { "callback": "reply:signal_id" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "editMessageText", "payload": { "text": "Signal not found." } }
        ]
      }
    ]
  }
]
